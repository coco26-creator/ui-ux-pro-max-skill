# Trae - uses .shared for data/scripts
# Trae uses skills folder structure similar to Claude

output: ".trae/skills/ui-ux-pro-max"
depends_on: [.shared]

files:
  - from: source/skill.md
    to: SKILL.md
    type: template
    vars:
      script_path: ".shared/ui-ux-pro-max/scripts/search.py"
